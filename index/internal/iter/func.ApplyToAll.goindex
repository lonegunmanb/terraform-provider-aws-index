package github.com/hashicorp/terraform-provider-aws/internal/iter
import (
	"iter"
)
func ApplyToAll[E1, E2 any](seq iter.Seq[E1], f func(E1) E2) iter.Seq[E2] {
	return func(yield func(E2) bool) {
		for v := range seq {
			if !yield(f(v)) {
				return
			}
		}
	}
}
