package github.com/hashicorp/terraform-provider-aws/internal/slices
import (
	"slices"
)
func Reverse[S ~[]E, E any](s S) S {
	n := len(s)
	v := S(make([]E, 0, n))

	for i := range n {
		v = append(v, s[n-(i+1)])
	}

	return v
}
