package github.com/hashicorp/terraform-provider-aws/internal/provider/framework/datasourceattribute
import (
	"sync"

	"github.com/hashicorp/terraform-plugin-framework/datasource/schema"
	"github.com/hashicorp/terraform-provider-aws/names"
)
var Region = sync.OnceValue(func() schema.StringAttribute {
	return schema.StringAttribute{
		Optional:    true,
		Computed:    true,
		Description: names.ResourceTopLevelRegionAttributeDescription,
	}
})
