package github.com/hashicorp/terraform-provider-aws/internal/sync
import (
	"sync"
)
func (m *Map[K, V]) Load(k K) (V, bool) {
	if a, b := m.m.Load(k); b {
		return a.(V), true
	} else {
		var zero V
		return zero, false
	}
}
