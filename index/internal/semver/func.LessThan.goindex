package github.com/hashicorp/terraform-provider-aws/internal/semver
import (
	"strconv"

	gversion "github.com/hashicorp/go-version"
)
func LessThan(s1, s2 string) bool {
	v1, v2, err := parseVersions(s1, s2)

	if err != nil {
		return false
	}

	return v1.LessThan(v2)
}
